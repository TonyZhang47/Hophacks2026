import { CommunityWorkspace } from "@/components/community/CommunityWorkspace";

export const metadata = { title: "Community — RxPlain" };

export default function CommunityPage() {
  return <CommunityWorkspace />;
}
